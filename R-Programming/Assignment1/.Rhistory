## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in id){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, i)
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
finalMean <- mean(vectorOfEachFilesMeans[i])
## print(finalMean)
finalMean
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean("specdata", "nitrate", 23)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in id){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, i)
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
finalMean <- mean(vectorOfEachFilesMeans)
## print(finalMean)
finalMean
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "nitrate", 23)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in id){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, i)
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
finalMean <- mean(vectorOfEachFilesMeans)
## print(finalMean)
finalMean
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "nitrate", 23)
source("pollutantmean.R")
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in id){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, i)
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
finalMean <- mean(vectorOfEachFilesMeans)
## print(finalMean)
finalMean
vectorOfEachFilesMeans
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
source("pollutantmean.R")
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "nitrate", 23)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in seq_along(id)){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, id[i])
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
finalMean <- mean(vectorOfEachFilesMeans)
## print(finalMean)
finalMean
vectorOfEachFilesMeans
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in seq_along(id)){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, id[i])
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
finalMean <- mean(vectorOfEachFilesMeans)
## print(finalMean)
finalMean
## vectorOfEachFilesMeans
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "nitrate", 70:72)
source("pollutantmean.R")
source("pollutantmean.R")
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## nameOfFile <- paste0(directory, "\\001.csv")
## pollutantmean <- nameOfFile
## Let's create our master vector that will cache all the individual files' means.
numberOfFilesToOpen <- length(id)
vectorOfEachFilesMeans <- vector("numeric", length = numberOfFilesToOpen)
sumOfAllNumbers <- 0
countOfAllNumbers <- 0
## Let's open each file, calculate the mean, and cache it in the master vector.
for (i in seq_along(id)){
## Open the file.
nameOfFile <- sprintf("%s\\%03d.csv", directory, id[i])
## print(nameOfFile)
currentFile <- read.csv(nameOfFile)
## Calculate the mean.
columnNameWeWant <- c(pollutant)
columnWeWant <- currentFile[columnNameWeWant]
rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
currentMean <- mean(rowsWeWant)
currentSum <- sum(rowsWeWant)
sumOfAllNumbers <- sumOfAllNumbers + currentSum
currentCount <- length(rowsWeWant)
countOfAllNumbers <- countOfAllNumbers + currentCount
## Cache it in the master vector.
vectorOfEachFilesMeans[i] <- currentMean
}
## OK, now, let's calculate the final mean.
## TODO: on the offchance that we have a NA in here, pull it out.
## finalMean <- mean(vectorOfEachFilesMeans)
finalMean <- sumOfAllNumbers / countOfAllNumbers
## print(finalMean)
finalMean
## vectorOfEachFilesMeans
## currentFile <- read.csv(nameOfFile, colClasses=c(pollutant="character"))
## currentFile <- read.csv(nameOfFile)
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## columnNameWeWant <- pollutant
## print(columnNameWeWant)
## columnNameWeWant <- c(pollutant)
## print(columnNameWeWant)
## columnWeWant <- currentFile[columnNameWeWant]
## rowsWeWant <- columnWeWant[!(is.na(columnWeWant))]
## Let's stuff the value of the current mean into our master vector.
## print(rowsWeWant)
## print(mean(rowsWeWant))
## i <- 1
## vectorOfEachFilesMeans[i] <- mean(rowsWeWant)
## print(vectorOfEachFilesMeans[i])
## print(columnWeWant)
## for (i in id){
##  print(currentFile[i])
## }
## currentFile
## myNewFile <- pollutantmean("c:\\users\\mikeconn\\skydrive\\(coursera)\\R-Programming\\Assignment1\\specdata", "nitrate", 1:10)
## Example Output:
## source("pollutantmean.R")
## pollutantmean("specdata", "sulfate", 1:10)
## ## [1] 4.064
## pollutantmean("specdata", "nitrate", 70:72)
## ## [1] 1.706
## pollutantmean("specdata", "nitrate", 23)
## ## [1] 1.281
}
pollutantmean("specdata", "nitrate", 70:72)
source ("pollutantmean.R")
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "sulfate", 1:10)
